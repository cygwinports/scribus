--- origsrc/scribus-1.3.6/scribus/scclocale.cpp	2010-02-11 17:04:54.000000000 -0600
+++ src/scribus-1.3.6/scribus/scclocale.cpp	2010-05-26 11:08:53.586688500 -0500
@@ -22,7 +22,7 @@ ScCLocale::ScCLocale()
 #if defined(Q_WS_WIN)
 	cLocale = _create_locale(LC_ALL, "C");
 #else
-  #if not defined(Q_OS_SOLARIS)
+  #if !defined(Q_OS_SOLARIS) && !defined(Q_OS_CYGWIN)
 	cLocale = newlocale(LC_ALL_MASK, "C", NULL);
   #endif
 #endif
@@ -33,7 +33,7 @@ ScCLocale::~ScCLocale()
 #if defined(Q_WS_WIN)
 	_free_locale(cLocale);
 #else
-  #if not defined(Q_OS_SOLARIS)
+  #if !defined(Q_OS_SOLARIS) && !defined(Q_OS_CYGWIN)
 	freelocale(cLocale);
   #endif
 #endif
@@ -108,7 +108,7 @@ double ScCLocale::strtod ( const char *
 #if defined(Q_WS_WIN)
 		return _strtod_l(str, endptr, that()->cLocale);
 #else
-  #if defined(Q_OS_SOLARIS)
+  #if defined(Q_OS_SOLARIS) || defined(Q_OS_CYGWIN)
 		char *oldlocale=setlocale(LC_NUMERIC, NULL);
 		double result(0.0);
 		setlocale(LC_NUMERIC, "C");
--- origsrc/scribus-1.3.6/scribus/scclocale.h	2010-02-09 17:29:55.000000000 -0600
+++ src/scribus-1.3.6/scribus/scclocale.h	2010-05-26 10:50:09.861415100 -0500
@@ -28,7 +28,7 @@
 #if defined(Q_WS_WIN)
 #define XLocaleType _locale_t
 #else
-  #if defined (Q_OS_SOLARIS)
+  #if defined (Q_OS_SOLARIS) || defined (Q_OS_CYGWIN)
   #define XLocaleType char*   //dummy?
   #else
   #define XLocaleType locale_t
